// Warmup actions hook - stub for migration
export const useWarmupFeedback = () => ({
  saveFeedback: async (data: any) => {
    console.log('useWarmupFeedback stub:', data);
  },
  isLoading: false
});